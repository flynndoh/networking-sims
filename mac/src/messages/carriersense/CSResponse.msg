message CSResponse {
    
    // This field is set to TRUE when the current signal power observed in channel exceeds the value of
    // the parameter csThreshDBm, otherwise it is set to FALSE.
	bool busyChannel;
	
}
